@model IEnumerable<Film>
@{ Layout = "_Layout"; ViewBag.Title = "Get a Film by ID";}

<form asp-action = "GetFilm" method="POST"> 
  <div class="form-group">
    <label for="FilmName">FilmName</label>
    <input type="text" class="form-control" id="FilmName" name="name" placeholder="Enter FilmName">
  </div>
@* <select class="custom-select" name = "name" id="inputGroupSelect01">
    <option selected>Choose...</option>
          @if(Model!=null){
          @foreach (var val in Model)
          {
             <option name = "name">@val.FilmName</option>
          }

      }
  </select>  *@

<button type="submit" class="btn btn-primary">Submit</button>
</form>


@if(Model!=null){

          <h2>Film</h2>
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>FilmName</th>
                <th>Director</th>
                <th>Hero</th>
                <th>Heroine</th>
                <th>Rating</th>
            </tr>
        </thead>
     @foreach (var i in Model)
     {

        <tbody>
            <tr>
                <td>@i.FilmName</td>
                <td>@i.Director</td>
                <td>@i.Hero</td>
                <td>@i.Heroine</td>
                <td>@i.Rating</td>
            </tr>
        </tbody>
     }

         </table>
 }